.venv/
__pycache__/
*.py[cod]
.env
.databricks/
.DS_Store
node_modules/
build/
*.log
capstone_app.egg-info/

# T8: the built React bundle (backend/static) is committed so the deployed
# git-source app runs `uvicorn backend.main:app` with no build step.
# (Do NOT ignore app/backend/static.)
